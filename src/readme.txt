TAREFAS:

------------------------------------------
-PONTO 2:  o servidor no populate -> decifrar o ficheiro com a key do mac que esta na keystore
		:  o servidor no accept de uma conexao tem de verificar ocertificado que o cliente esta a mandar com o que esta na sua keystore

-PONTO 3:configurar as chaves tanto no cliente(truststore com o certificado do servidor) como no servidor (keystore com a sua chave privada).

-PONTO 4: CIFRAR FICHEIROS

-PONTO 5: CIFRAR FICHEIROS

------------------------------------------